<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.tonyqwe.cinemaweb.mapper.UserWalletMapper">

    <select id="selectByUserId" resultType="org.tonyqwe.cinemaweb.domain.entity.UserWallet">
        SELECT * FROM user_wallets WHERE user_id = #{userId}
    </select>

</mapper>
