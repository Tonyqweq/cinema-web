<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.tonyqwe.cinemaweb.mapper.MovieReviewMapper">
    
    <select id="getReviewsByMovieId" resultType="org.tonyqwe.cinemaweb.domain.entity.MovieReview">
        SELECT * FROM movie_reviews 
        WHERE movie_id = #{movieId} 
        ORDER BY created_at DESC
    </select>
    
    <select id="getReviewByUserAndMovie" resultType="org.tonyqwe.cinemaweb.domain.entity.MovieReview">
        SELECT * FROM movie_reviews 
        WHERE user_id = #{userId} AND movie_id = #{movieId}
    </select>
    
    <select id="getAverageRatingByMovieId" resultType="java.lang.Double">
        SELECT AVG(rating) FROM movie_reviews 
        WHERE movie_id = #{movieId}
    </select>
    
    <select id="getReviewCountByMovieId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM movie_reviews 
        WHERE movie_id = #{movieId}
    </select>
</mapper>
